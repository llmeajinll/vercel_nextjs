export const handleState = async (id: string, state: boolean) => {
  // console.log(todos, id, !state);
  //   setTodos((prev: any) => {
  //     return prev.map((item: TodoType) => {
  //       if (item._id === id) {
  //         return { ...item, state: state };
  //       }
  //       return item;
  //     });
  //   });
};
